namespace = vg1_bridge

#Spawn anomaly for void governor

planet_event = {	
	id = vg1_bridge.1
	is_triggered_only = yes
	hide_window = yes

	trigger = {
		has_planet_flag = void_governor_planet
	}

	immediate = {
		add_anomaly = {
			category = void_governor_category
			target = from
		}
	}
}