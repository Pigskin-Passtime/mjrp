
namespace = twll_gamestart

event = {					# set allowed and installed for spawning module
	id = twll_gamestart.100
	is_triggered_only = yes
	hide_window = yes
	fire_only_once = yes

	trigger = {
		
	}

	immediate = {
		set_global_flag = twll_installed
		set_global_flag = twll_allowed

	}
}
