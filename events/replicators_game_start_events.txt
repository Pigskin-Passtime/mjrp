namespace = replicators_game_start

# Global flag that Replicators is installed/active.
event = {
	id = replicators_game_start.1
	is_triggered_only = yes
	hide_window = yes
	immediate = {
		set_global_flag = replicators_installed # Detects the submission.
		set_global_flag = replicators_allowed # Required for spawning.
	}
}