
namespace = mem_gamestart

event = {					# set allowed and installed for spawning module
	id = mem_gamestart.100
	is_triggered_only = yes
	hide_window = yes
	fire_only_once = yes

	trigger = {}

	immediate = {
		set_global_flag = mem_installed
		set_global_flag = mem_allowed

	}
}
