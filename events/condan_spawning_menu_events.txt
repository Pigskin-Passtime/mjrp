
namespace = condan_gamestart

event = {					# set allowed and installed for spawning module
	id = condan_gamestart.100
	is_triggered_only = yes
	hide_window = yes
	fire_only_once = yes

	trigger = {}

	immediate = {
		set_global_flag = condan_installed
		set_global_flag = condan_allowed

	}
}
