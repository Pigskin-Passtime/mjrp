#########################
#
# Tiyanki Resurgence Crisis; game_start_events
#
# Written by RegiZero
#
#########################

namespace = tiyres_game_start

event = { 
	id = tiyres_game_start.1
	is_triggered_only = yes
	hide_window = yes

	trigger = {}

	immediate = {
		set_global_flag = tiyres_installed							# required for the Mod Jam mod to detect your submission
		set_global_flag = tiyres_allowed							# required for spawning 
	}
}
