namespace = TILL_gamestart

event = {					# set allowed and installed for spawning module
	id = TILL_gamestart.100
	is_triggered_only = yes
	hide_window = yes
	fire_only_once = yes

	trigger = {
		Fragement_trigger = yes
	}

	immediate = {
		set_global_flag = TILL_installed
		set_global_flag = TILL_allowed

	}
}
