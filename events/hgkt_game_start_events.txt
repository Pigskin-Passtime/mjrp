#########################################################################################
# Thayo Everdawn - The Everdawn Heist
# Author: TheRingBearer
#########################################################################################

namespace = hskv_gamestart

# Alright Freake, the wrapper is gone now. I'll just use this to do my spawn for testing
country_event = {
	id = hskv_gamestart.1
	is_triggered_only = yes
	hide_window = yes

	immediate = {
		if = {
			limit = {
				NOT = {
					has_global_flag = hskv_system_spawned
				}
			}
			capital_scope = {
				solar_system = {
					random_neighbor_system = {
						limit = {
							distance = {
								min_jumps = 1
								max_jumps = 1
							}
						}
						hskv_spawn = yes
					}
				}
			}
		}
	}
}