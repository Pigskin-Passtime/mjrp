mgo_crisis_research_lab = {
	can_build = yes
	base_buildtime = @b1_time
	exempt_from_ai_planet_specialization = no

	category = research

	building_sets = {
		research
	}

	potential = {
		NOR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
		exists = owner
		owner = {
			has_crisis_perk = occultist_alternative_approach
			is_wilderness_empire = no
		}
		is_special_colony_type = no
	}

	allow = {
	}

	ai_weight_coefficient = 0

	destroy_trigger = {
		OR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
	}

	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = { has_crisis_perk = occultist_alternative_approach }
		}
		job_mgo_crisis_occultist_add = 200
	}

	resources = {
		category = planet_buildings
		cost = {
			minerals = @b1_minerals
		}
		upkeep = {
			energy = @b1_upkeep
		}
	}
}

mgo_crisis_hellforge = {
	can_build = yes
	base_buildtime = @b1_time
	exempt_from_ai_planet_specialization = no

	category = research

	building_sets = {
		research
	}

	planet_limit = 1

	potential = {
		NOR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
		exists = owner
		owner = {
			has_crisis_perk = occultist_unholy_covenant_2
			has_country_flag = mgo_crisis_infernal
			is_wilderness_empire = no
		}
		is_special_colony_type = no
	}

	allow = {
	}

	ai_weight_coefficient = 0

	destroy_trigger = {
		OR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
	}

	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = { has_crisis_perk = occultist_alternative_approach }
		}
		job_mgo_crisis_occultist_add = 100
	}

	resources = {
		category = planet_buildings
		cost = {
			minerals = @b1_minerals
		}
		upkeep = {
			energy = @b1_upkeep
		}
	}
}

mgo_crisis_divination_chamber = {
	can_build = yes
	base_buildtime = @b1_time
	exempt_from_ai_planet_specialization = no

	category = research

	building_sets = {
		research
	}

	planet_limit = 1

	potential = {
		NOR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
		exists = owner
		owner = {
			has_crisis_perk = occultist_unholy_covenant_2
			has_country_flag = mgo_crisis_psionic
			is_wilderness_empire = no
		}
		is_special_colony_type = no
	}

	allow = {
	}

	ai_weight_coefficient = 0

	destroy_trigger = {
		OR = {
			has_modifier = resort_colony
			has_modifier = slave_colony
			has_modifier = penal_colony
		}
	}

	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = { has_crisis_perk = occultist_alternative_approach }
		}
		job_mgo_crisis_occultist_add = 100
	}

	resources = {
		category = planet_buildings
		cost = {
			minerals = @b1_minerals
		}
		upkeep = {
			energy = @b1_upkeep
		}
	}
}