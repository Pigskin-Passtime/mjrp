
narrow_escape_consolidate_colony_ships = {
	length = 0
	icon = "GFX_edict_type_policy"
	edict_cap_usage = 0

	potential = {
		check_variable = {
			which = narrow_escape_origin_colony_pops_left
			value >= 16
		}
	}

	allow = {
		custom_tooltip = {
			fail_text = "Consolidation menu is already open!"
			NOT = { has_country_flag = narrow_escape_origin_consolidation_menu_open }
		}
	}

	effect = {
		set_country_flag = narrow_escape_origin_consolidation_menu_open
		country_event = { id = narrow_escape.20 }
	}
}
