# Evergrowth Protocol
# By Wiirlak


@bloomMyco = 40
@bloomMycoCostly = 100

@bloomCost = 7500
@bloomCostHigh = 12000

@bloomTime = 3600
@highestTime = 9000

# Bloom World
terraform_link = {
	from = "pc_desert"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_arid"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_savannah"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}
	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_arctic"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_tundra"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_alpine"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_continental"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}
	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_tropical"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 1
	}
}

terraform_link = {
	from = "pc_ocean"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMyco
		}
	}

	duration = @bloomTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 2
		modifier = { #Ocean Worlds are Aquatic species' paradises
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						OR = {
							has_trait = trait_aquatic
							has_trait = trait_robot_aquatic
						}
					}
				}
			}
		}
	}
}

terraform_link = {
	from = "pc_barren"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
				from = { has_modifier = terraforming_candidate }
			}
		}
	}

	condition = {
		has_technology = "tech_climate_restoration"
	}

	effect = {
		from = { remove_modifier = terraforming_candidate }
	}

	ai_weight = {
		weight = 10
	}
}

terraform_link = {
	from = "pc_barren_cold"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
				from = { has_modifier = terraforming_candidate }
			}
		}
	}

	condition = {
		has_technology = "tech_climate_restoration"
	}

	effect = {
		from = { remove_modifier = terraforming_candidate }
	}

	ai_weight = {
		weight = 10
	}
}

terraform_link = {
	from = "pc_nuked"
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	condition = {
		has_technology = "tech_climate_restoration"
	}

	effect = {
		from = { remove_modifier = terraforming_candidate }
	}

	ai_weight = {
		weight = 10
	}
}

terraform_link = {
	from = pc_hive
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						OR = {
							has_trait = trait_hive_mind
							has_trait = trait_pc_hive_preference
						}
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_machine
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						OR = {
							has_trait = trait_machine_unit
							has_trait = trait_machine_habitat_planet_preference
						}
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_nanotech
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						has_trait = trait_machine_pc_nanotech_preference
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_relic
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						OR = {
							has_trait = trait_pc_relic_preference
							has_trait = trait_machine_pc_relic_preference
						}
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_city
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						has_trait = trait_pc_city_preference
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_gaia
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCost
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		OR = {
			FROM = {
				OR = {
					has_planet_flag = terraform_nucleus_transfixed
					has_planet_flag = wiirlak_ep_forcibly_bloomed
					has_planet_flag = wiirlak_ep_diplomatically_bloomed
				}
			}
			AND = {
				exists = owner
				has_technology = wiirlak_ep_tech_bloom_colonization_1
			}
		}
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						OR = {
							has_trait = trait_pc_gaia_preference
							has_trait = trait_pc_gaia_preference_terraforming
							has_trait = trait_machine_pc_gaia_preference
						}
					}
				}
			}
		}
	}
}

terraform_link = {
	from = pc_toxic
	to = wiirlak_ep_pc_bloom

	resources = {
		category = terraforming
		inline_script = {
			script = "wiirlak_ep_food_terraform_cost"
			AMOUNT = @bloomCostHigh
			MYCO = @bloomMycoCostly
		}
	}

	duration = @highestTime

	potential = {
		FROM = { has_modifier = toxic_terraforming_candidate }
		AND = {
			exists = owner
			has_technology = wiirlak_ep_tech_bloom_colonization_1
		}
	}
	condition = {
		has_ascension_perk = ap_detox
		has_technology = tech_climate_restoration
	}

	effect = {
		from = { detox_hangover = yes }
	}

	ai_weight = {
		weight = 10
		modifier = {
			factor = 0.5
			from = {
				is_colony = yes
				pop_amount_percentage = {
					percentage >= 0.5
					limit = {
						has_trait = trait_pc_city_preference
					}
				}
			}
		}
	}
}
