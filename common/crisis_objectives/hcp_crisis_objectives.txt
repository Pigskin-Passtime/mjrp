##################
# hcp_crisis_objectives.txt
# By jam1066
##################

hcp_crisobj_build_mining_stations = {
	reward = {
		base = 10000
		divide = trigger:num_galaxy_systems 	#on huge, this is 10. On smallest, it is 50
		round = yes 							#we don't want 3.124 mining efficiency
		min = 10								#just in case
	}
}

hcp_crisobj_scavenge_debris = {
	reward = {
		base = 10000
		divide = trigger:num_galaxy_systems 	#on huge, this is 10. On smallest, it is 50
		round = yes 							#we don't want 3.124 mining efficiency
		min = 10								#just in case
	}
}

hcp_crisobj_own_systems = {
	reward = {
		base = 10000
		divide = trigger:num_galaxy_systems 	#on huge, this is 10. On smallest, it is 50
		round = yes 							#we don't want 3.124 mining efficiency
		min = 10								#just in case
	}
}

hcp_crisobj_harvest_planets = {
	reward = {
		base = 50000
		divide = trigger:num_galaxy_systems 	#on huge, this is 1000. On smallest, it is 5000
		round_to = 50 								#we don't want 127.5 mining efficiency
		min = 50									#just in case
	}
}

hcp_crisobj_harvest_star_systems = {
	reward = {
		base = 1000000
		divide = trigger:num_galaxy_systems 	#on huge, this is 1000. On smallest, it is 250
		round_to = 500 								#we don't want 127.5 mining efficiency
		min = 1000									#just in case
	}
}

hcp_crisobj_have_arc_furnaces = {
	potential = {
		has_machine_age_dlc = yes
	}
	reward = {
		base = 400
		complex_trigger_modifier = { #subtract 50 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 25
		}
		round_to = 50 	#we want a solid number
		min = 50		#and we don't want less than 200
	}
	recurring = yes
}

hcp_crisobj_galcom_industrial_resolution = {
	#potential = {
	#	has_federations_dlc = yes
	#}
	reward = {
		base = 100
		complex_trigger_modifier = { #subtract 2.5 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 2.5
		}
		round_to = 10 	#we want a solid number
		min = 50 		#and we don't want less than 50
	}
	recurring = yes
}

hcp_crisobj_have_matter_decompressors = {
	potential = {
		has_megacorp = yes
	}
	reward = {
		base = 800
		complex_trigger_modifier = { #subtract 50 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 50
		}
		round_to = 200 	#we want a solid number
		min = 200		#and we don't want less than 200
	}
	recurring = yes
}

hcp_crisobj_have_prospectorium_vassals = {
	potential = {
		has_overlord_dlc = yes
	}
	reward = {
		base = 250
		complex_trigger_modifier = { #subtract 10 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 10
		}
		round_to = 50 	#we want a solid number
		min = 50		#and we don't want less than 50
	}
	recurring = yes
}