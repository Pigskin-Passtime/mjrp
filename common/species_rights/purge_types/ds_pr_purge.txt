# from = country
# this = species

purge_ds_pr_harvest = {
	pop_escape_chance = 0
	pop_decline_rate = 150 # per month
	pop_group_modifier = { pop_happiness = -1 }
	potential = {
		exists = from
		from = {
			is_ai = no
			has_ascension_perk = ap_ds_phantom_reality
		}
	}
	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = same_species_disallowed
			NOT = { is_same_species = from }
		}
		NOR = {
			has_trait = trait_mechanical
			has_trait = trait_machine_unit
		}
	}
	ai_will_do = {
		factor = 25
		modifier = {
			factor = 0
			OR = {
				has_trait = trait_mechanical
				has_trait = trait_machine_unit
			}
		}
	}
}
