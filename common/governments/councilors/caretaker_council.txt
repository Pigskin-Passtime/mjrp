councilor_caretaker_first_monitor = {
	leader_class = {
		scientist
		official
		commander
	}
	is_leader_possible = {
		OR = {
			leader_class = scientist
			leader_class = official
			leader_class = commander
		}
	}
	possible = {
		has_origin = origin_caretaker
		OR = {
			has_valid_civic = civic_care_crusade
			has_valid_civic = civic_monitor_cult
			has_valid_civic = civic_one_big_family
			has_valid_civic = civic_protected
			has_valid_civic = civic_drone_culture
			has_valid_civic = civic_love_the_alien
		}
	}
	icon = GFX_origin_caretaker_first_monitor
	# civic = civic_monitor_cult
	# is_leader_possible = {
	# 	has_trait
	# }
	modifier = {
		planet_entertainers_unity_produces_add = 0.25
		planet_bureaucrats_unity_produces_add = 0.25
	}
	ai_hiring_weight = {
		inline_script = councilor_leader_weights
	}
}
