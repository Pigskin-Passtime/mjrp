#Decision by nepnep5150 and newjerseyprpr

decision_Yuthaan_Planetary_Expansion = {
	owned_planets_only = yes

	enactment_time = 360
	resources = {
		category = decisions
		cost = {
			exotic_gases = 200
			energy = 500
		}
	}

	potential = {
		habitable_structure = no
		NOT = { is_planet_class = pc_machine }
		NOT = { is_planet_class = pc_hive }
		NOT = { is_planet_class = pc_city }
		owner = {
			has_country_flag = synthmind_yuthaan_decision
		}
	}

	allow = {
		has_planet_flag = synthmind_yuuthan_twice
	}

	effect = {
		custom_tooltip = decision_Yuthaan_Planetary_Expansion
		hidden_effect = { 
			if = {
				limit = { has_planet_flag = synthmind_yuuthan_once }
				set_planet_flag = synthmind_yuuthan_twice
			}
			else = {
				set_planet_flag = synthmind_yuuthan_once
			}
			
			add_random_non_blocker_deposit = yes
			add_random_non_blocker_deposit = yes

		}
	}
}